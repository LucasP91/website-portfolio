{
  "_note": "User-scope MCP servers registered this session (live copy is in ~/.claude.json under top-level mcpServers). The Magic API key is REDACTED here for safety; the real value lives in ~/.claude.json and should be rotated at https://21st.dev/magic/console since it was shared in chat.",
  "mcpServers": {
    "magic": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@21st-dev/magic@latest", "API_KEY=<REDACTED — see ~/.claude.json>"]
    },
    "motion-dev": {
      "type": "stdio",
      "command": "C:\\Users\\Lucas\\nodejs\\node.exe",
      "args": ["C:\\Users\\Lucas\\motion-dev-mcp\\dist\\index.js"],
      "cwd": "C:\\Users\\Lucas\\motion-dev-mcp"
    }
  }
}
